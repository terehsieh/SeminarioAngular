export interface Product{
    // export declararla de manera publica
    name: string;
    type: string;
    price: number;
    stock: number;
    image: string; // es una url
    clearance: boolean; // oferta

}